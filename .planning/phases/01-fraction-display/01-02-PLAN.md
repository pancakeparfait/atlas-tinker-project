---
phase: 01-fraction-display
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/recipes/import-review.tsx
autonomous: true

must_haves:
  truths:
    - 'User imports recipe from URL and sees fractions in review page before saving'
    - 'Fractions display consistently with recipe detail page formatting'
    - 'Import review confidence indicators continue functioning correctly'
  artifacts:
    - path: 'src/components/recipes/import-review.tsx'
      provides: 'Import review component with fraction preview display'
      min_lines: 280
      contains: 'formatQuantityAsFraction'
  key_links:
    - from: 'src/components/recipes/import-review.tsx'
      to: 'src/lib/utils.ts'
      via: 'import formatQuantityAsFraction'
      pattern: 'import.*formatQuantityAsFraction.*from.*@/lib/utils'
---

<objective>
Add fraction formatting to the recipe import review preview so users see natural fractions when reviewing parsed ingredients before saving the recipe.

Purpose: Provides immediate visual feedback during import that matches the final recipe display, improving import workflow consistency.

Output: Import review component showing fraction-formatted quantities in the preview section.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fraction-display/01-CONTEXT.md
@.planning/phases/01-fraction-display/01-RESEARCH.md
@src/lib/utils.ts
@src/components/recipes/import-review.tsx
@src/components/recipes/ingredient-list.tsx
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Add fraction formatting to import review preview</name>
  <files>src/components/recipes/import-review.tsx</files>
  <action>
The ImportReview component uses the IngredientList component (line 275) which is a form input component. The preview display happens BEFORE the form, showing the raw imported data.

Research shows the component structure:

- Lines 1-99: Setup and form initialization
- Lines 100-308: Render section with preview

ACTION:

1. Add import at top of file (around line 15):

   ```typescript
   import { formatQuantityAsFraction } from '@/lib/utils'
   ```

2. Search for where imported ingredients are displayed in preview mode (NOT the IngredientList form component).

3. If there's a preview section showing parsed ingredients before the form:
   - Apply formatQuantityAsFraction to quantity display
   - Use same pattern as Plan 01: `formatQuantityAsFraction(parseFloat(ing.quantity))`

4. If NO preview section exists (IngredientList is only display):
   - Document that import review uses form inputs which should accept decimals per Phase 1 scope
   - Add TODO comment for future enhancement: "// TODO Phase 2: Show fraction preview of imported quantities before edit"
   - NO CODE CHANGES needed in this case

IMPORTANT:

- IngredientList component (imported and used) is a FORM component - do NOT modify its display (inputs should accept decimals)
- Only modify read-only preview display if it exists
- Use @/ path alias for imports
- Preserve all confidence indicator logic
- 'use client' directive already present
  </action>
  <verify>

1. Check if import added: `grep "formatQuantityAsFraction" src/components/recipes/import-review.tsx`
2. Verify no changes to IngredientList form inputs: `git diff src/components/recipes/ingredient-list.tsx` should show no changes
3. Build succeeds: `pnpm build`
4. TypeScript compiles: `pnpm type-check`
   </verify>
   <done>
   Import review component updated appropriately:

- If preview exists: Fractions display in preview section
- If preview doesn't exist: Documented as future enhancement, no changes needed
- IngredientList form component unchanged (correctly accepts decimal inputs)
- Build and type-check pass with no errors
  </done>
  </task>

<task type="auto">
  <name>Manual validation of import workflow</name>
  <files>N/A (manual testing task)</files>
  <action>
Start dev server and test the import workflow end-to-end:

```bash
pnpm dev
```

Test procedure:

1. Navigate to http://localhost:3000/recipes/new
2. Enter a recipe URL (any common recipe site: allrecipes.com, foodnetwork.com, etc.)
3. Click "Import Recipe" and wait for parsing
4. On import review page, observe:
   - How ingredient quantities are displayed
   - Whether they show as fractions or decimals
   - Whether the form inputs accept decimal entry

5. Document findings:
   - If quantities show as fractions in preview: SUCCESS - meets FRAC-02
   - If quantities only in form inputs (no preview): DOCUMENTED - future enhancement
   - Verify confidence indicators still work correctly
   - Verify import completion works (click Accept & Continue Editing, recipe saves)

6. Create a test report comment in code or SUMMARY documenting:
   - What was tested
   - Current behavior
   - Whether FRAC-02 is met or deferred

IMPORTANT:

- This is documentation/verification task, not implementation
- Goal is to understand current import review UX and document state
- Don't force changes if preview section doesn't exist
  </action>
  <verify>

1. Dev server runs: `curl -I http://localhost:3000` returns 200
2. Import workflow completes without errors
3. Test results documented (in code comment or ready for SUMMARY)
   </verify>
   <done>
   Import review workflow tested and documented. Current state clearly identified. Either FRAC-02 met (preview shows fractions) or documented as future work (no preview section exists).
   </done>
   </task>

</tasks>

<verification>
1. Import review component builds without errors
2. Form inputs still accept decimal quantities (not affected by display changes)
3. Import workflow functions correctly end-to-end
4. Current state of FRAC-02 requirement documented
</verification>

<success_criteria>

- FRAC-02: Import review displays fractions (if preview exists) OR documented as future enhancement ✓
- Form inputs continue accepting decimal quantities ✓
- No breaking changes to import workflow ✓
- Build and type-check succeed ✓
- Manual import test completes successfully ✓
  </success_criteria>

<output>
After completion, create `.planning/phases/01-fraction-display/01-02-SUMMARY.md`
</output>
