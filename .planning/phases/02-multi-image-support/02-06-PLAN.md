---
phase: 02-multi-image-support
plan: 06
type: execute
wave: 4
depends_on:
  - "02-02"
  - "02-03"
files_modified:
  - src/app/recipes/page.tsx
  - src/app/recipes/__tests__/page.test.tsx
autonomous: true
requirements:
  - IMG-07
must_haves:
  truths:
    - "Each recipe Card in the grid shows a primary thumbnail loaded from /api/recipes/[id]/images/[primaryImageId] when primaryImageId is set"
    - "Recipes without a primaryImageId render a placeholder Card header with a Utensils icon"
    - "Recipe list cards display each recipe's primary image preview above the title at a consistent size that does not break the grid layout"
  artifacts:
    - path: "src/app/recipes/page.tsx"
      provides: "Recipe list/grid with primary thumbnail or Utensils placeholder per card"
      contains: "primaryImageId"
    - path: "src/app/recipes/__tests__/page.test.tsx"
      provides: "Test that recipes-with-primary render thumbnail; recipes-without render Utensils placeholder"
      contains: "primaryImageId"
  key_links:
    - from: "src/app/recipes/page.tsx Card"
      to: "/api/recipes/[id]/images/[primaryImageId]"
      via: "<Image> src template literal"
      pattern: "/api/recipes/.*/images/"
---

<objective>
Render the primary image thumbnail (or a placeholder) on every Card in the recipe list/grid. After this plan, IMG-07 is closed — users see a visual preview of each recipe before clicking into it.

Purpose: This is the last user-facing surface for Phase 2. The list endpoint already includes `primaryImageId` per recipe (plan 02 — list endpoint modification), and the Recipe interface in recipe-queries.ts already types it (plan 03), so this plan only adds the rendering layer.

Output: A surgical edit to the recipes list page and an accompanying test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-multi-image-support/02-CONTEXT.md
@.planning/phases/02-multi-image-support/02-UI-SPEC.md
@.planning/phases/02-multi-image-support/02-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/app/recipes/page.tsx
@.planning/phases/02-multi-image-support/02-02-SUMMARY.md
@.planning/phases/02-multi-image-support/02-03-SUMMARY.md

<interfaces>
<!-- Existing context: Recipe interface (extended in plan 03) exposes primaryImageId from the list endpoint (set by plan 02). -->

```typescript
interface Recipe {
  // ...existing fields...
  primaryImageId?: string | null;
}
```

<!-- No new exports from this plan. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render primary thumbnail or Utensils placeholder per Card in the recipe list</name>
  <read_first>
    - src/app/recipes/page.tsx (entire file — Card rendering at lines 151-213; existing lucide imports at line 10)
    - .planning/phases/02-multi-image-support/02-UI-SPEC.md (Surface 5 — Recipe List Thumbnail)
    - .planning/phases/02-multi-image-support/02-PATTERNS.md ("src/app/recipes/page.tsx" section)
    - src/components/ui/card.tsx (CardHeader signature to position thumbnail correctly)
    - src/app/recipes/__tests__/page.test.tsx if it exists; otherwise note absence
  </read_first>
  <behavior>
    - Test A — recipe with primaryImageId renders thumbnail: Card for recipe `{ id: 'r1', title: 'Tacos', primaryImageId: 'img-1' }` contains an `<img>` element with `src="/api/recipes/r1/images/img-1"`, `width="160"`, `height="120"`, and className containing `object-cover` and `rounded-t-lg`.
    - Test B — recipe without primaryImageId renders Utensils placeholder: Card for recipe `{ id: 'r2', title: 'Soup', primaryImageId: null }` does NOT contain an `<img>` to `/api/recipes/r2/images/...`. It DOES contain an SVG/icon element identified by the `lucide-utensils` class (or testable via `screen.getByLabelText` or the data attribute that lucide adds).
    - Test C — alt text uses recipe title: the thumbnail's `alt` attribute equals the recipe title (`Tacos` in Test A).
  </behavior>
  <action>
    Edit src/app/recipes/page.tsx:

    1) Add `Utensils` to the existing lucide-react import at line 10 (or wherever lucide-react is imported). Example: `import { Plus, Search, Utensils } from 'lucide-react'` (preserve existing icons; only add `Utensils`).

    2) Add `Image` to the imports if not already present: `import Image from 'next/image'`. (Check existing imports first — if the page already uses next/image, skip; PATTERNS.md says no existing usage on this page.)

    3) Locate the Card-rendering JSX block (PATTERNS identifies lines 151-213). Find the `<CardHeader>` element. Insert the following block IMMEDIATELY BEFORE the `<CardHeader>`:
       ```
       {recipe.primaryImageId ? (
         <Image
           src={`/api/recipes/${recipe.id}/images/${recipe.primaryImageId}`}
           alt={recipe.title}
           width={160}
           height={120}
           className="w-full h-30 object-cover rounded-t-lg"
         />
       ) : (
         <div
           className="w-full h-30 bg-secondary rounded-t-lg flex items-center justify-center"
           aria-label={`No photo for ${recipe.title}`}
         >
           <Utensils className="h-8 w-8 text-muted-foreground" aria-hidden />
         </div>
       )}
       ```

       Dimensions and classes per UI-SPEC Surface 5: 160×120, `object-cover`, `rounded-t-lg`. The placeholder reuses the same height utility (`h-30`) and the secondary background.

    4) Do NOT change any other Card content (title, description, badges, click handler).

    Create or extend src/app/recipes/__tests__/page.test.tsx:
    - If the test file does not exist, scaffold per TESTING.md (mock useRouter/useRecipes; the page uses useRecipes for the list — confirm by reading page.tsx imports). The hook mock pattern:
      ```
      jest.mock('@/lib/queries/recipe-queries', () => ({
        useRecipes: jest.fn(),
      }))
      ```
      Then in tests configure:
      ```
      (useRecipes as jest.Mock).mockReturnValue({
        data: { recipes: [
          { id: 'r1', title: 'Tacos', primaryImageId: 'img-1', /* minimum required Recipe fields per page */ },
          { id: 'r2', title: 'Soup', primaryImageId: null, /* minimum required Recipe fields */ },
        ], pagination: { page: 1, limit: 20, total: 2, pages: 1 } },
        isLoading: false,
        error: null,
      })
      ```
      Use TESTING.md `next/image` mock so `<Image>` becomes `<img>`.

    - Test A: render, assert `screen.getByAltText('Tacos')` exists with `src` ending in `/images/img-1`; assert `width="160"` and `height="120"`.
    - Test B: assert `screen.queryByAltText('Soup')` is null; assert there is an element matching the placeholder. Lucide icons render as SVGs with `class="lucide lucide-utensils"` — use `document.querySelector('.lucide-utensils')` or `screen.getByLabelText('No photo for Soup')` (the aria-label on the wrapper makes it discoverable).
    - Test C: covered by Test A's alt-text assertion.

    Constraints:
    - The `<Image>` from next/image still requires explicit width/height (Pitfall 3) — these are set.
    - Do NOT loosen any existing test (if the test file already exists and has list-rendering tests, only ADD the new assertions; do not remove any).
    - Do NOT add a new placeholder image asset — Utensils icon is the placeholder per UI-SPEC Surface 5.
  </action>
  <verify>
    <automated>pnpm test --testPathPattern="src/app/recipes/__tests__/page" --silent 2>&1 | tail -30 && pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Tests A-C PASS
    - `grep -E "primaryImageId" src/app/recipes/page.tsx` matches at least twice (ternary check + interpolation)
    - `grep -E "Utensils" src/app/recipes/page.tsx | wc -l` >= 2 (import + JSX usage)
    - `grep -E "width=\\{160\\}.*height=\\{120\\}" src/app/recipes/page.tsx` matches (or matching lines via dotall regex)
    - `grep -E "rounded-t-lg" src/app/recipes/page.tsx` matches
    - `pnpm exec tsc --noEmit` clean
    - Full suite `pnpm test` reports green across all phase-2 plans
  </acceptance_criteria>
  <done>Recipe grid renders primary thumbnail with correct dimensions and placeholder for recipes without images; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| List response → Card render | `primaryImageId` from server interpolated into `<Image src>` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-24 | Information Disclosure | List page thumbnail src | mitigate | `primaryImageId` only — the recipe list response carries NO image bytes (plan 02 T-02-11). Binary fetch goes to the per-image endpoint which enforces ownership server-side. |
| T-02-25 | Availability | Missing primaryImageId crash | mitigate | The ternary guards null/undefined explicitly; placeholder render path covers absence |
</threat_model>

<verification>
- Recipe list test asserts thumbnail rendering for recipes with primaryImageId
- Recipe list test asserts Utensils placeholder for recipes without
- `pnpm exec tsc --noEmit` clean
- Full test suite green
</verification>

<success_criteria>
- Recipe grid Cards show a 160×120 thumbnail when primary image exists
- Cards without a primary image show the Utensils placeholder in the same slot
- No regression on existing Card behavior (title, description, navigation)
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-image-support/02-06-SUMMARY.md` listing: total Phase 2 test count delta (baseline 111 → final), any cross-cutting cleanup observed (e.g., the legacy /api/recipes/[id]/image route is still in place — recommend its removal as a follow-up phase), and confirmation that all seven IMG-* requirements are observably satisfied end-to-end.
</output>
