---
phase: 02-multi-image-support
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/app/api/recipes/[id]/images/route.ts
  - src/app/api/recipes/[id]/images/[imageId]/route.ts
  - src/app/api/recipes/[id]/images/reorder/route.ts
  - src/app/api/recipes/[id]/images/__tests__/route.test.ts
  - src/app/api/recipes/[id]/images/[imageId]/__tests__/route.test.ts
  - src/app/api/recipes/[id]/images/reorder/__tests__/route.test.ts
  - src/app/api/recipes/route.ts
  - src/app/api/recipes/[id]/route.ts
  - src/app/api/recipes/__tests__/route.test.ts
  - src/app/api/recipes/[id]/__tests__/route.test.ts
autonomous: true
requirements:
  - IMG-01
  - IMG-02
  - IMG-03
  - IMG-04
  - IMG-06
  - IMG-07
tags:
  - api
  - next-route-handler
  - prisma
  - multipart
  - zod
must_haves:
  truths:
    - "POST /api/recipes/[id]/images accepts multipart form data with one or more `images` parts and creates one RecipeImage row per valid file"
    - "GET /api/recipes/[id]/images returns a JSON list of image metadata (id/order/fileName/mimeType) sorted ascending by order"
    - "GET /api/recipes/[id]/images/[imageId] returns the raw bytes with Content-Type and Cache-Control headers; returns 404 when the imageId is not owned by the recipeId"
    - "DELETE /api/recipes/[id]/images/[imageId] removes the row and renormalizes remaining order values"
    - "PATCH /api/recipes/[id]/images/reorder accepts { orderedIds: string[] } and updates order values atomically, rejecting cross-recipe ids"
    - "GET /api/recipes (list) includes primaryImageId for each recipe sourced from the order=0 RecipeImage row, with no `data` bytes in the response"
    - "GET /api/recipes/[id] (detail) includes a `images` array sorted by order with metadata only, no bytes"
  artifacts:
    - path: "src/app/api/recipes/[id]/images/route.ts"
      provides: "GET (list metadata) + POST (multi-file upload) handlers"
      exports: ["GET", "POST"]
    - path: "src/app/api/recipes/[id]/images/[imageId]/route.ts"
      provides: "GET (binary) + DELETE handlers"
      exports: ["GET", "DELETE"]
    - path: "src/app/api/recipes/[id]/images/reorder/route.ts"
      provides: "PATCH reorder handler with zod validation"
      exports: ["PATCH"]
    - path: "src/app/api/recipes/[id]/images/__tests__/route.test.ts"
      provides: "Unit tests covering POST validation, list shape, ownership guard, T-02-12 concurrency"
      contains: "describe"
    - path: "src/app/api/recipes/[id]/images/[imageId]/__tests__/route.test.ts"
      provides: "Unit tests for binary GET and DELETE with renormalization"
      contains: "describe"
    - path: "src/app/api/recipes/[id]/images/reorder/__tests__/route.test.ts"
      provides: "Unit tests for PATCH reorder including zod-error and cross-recipe rejection"
      contains: "describe"
  key_links:
    - from: "POST /api/recipes/[id]/images"
      to: "storageAdapter.saveRecipeImage"
      via: "loop over formData.getAll('images')"
      pattern: "saveRecipeImage"
    - from: "DELETE /api/recipes/[id]/images/[imageId]"
      to: "storageAdapter.deleteRecipeImage"
      via: "transactional delete + renormalize"
      pattern: "deleteRecipeImage"
    - from: "PATCH .../reorder"
      to: "storageAdapter.reorderRecipeImages"
      via: "zod parse -> array of prisma.update inside $transaction"
      pattern: "reorderRecipeImages"
    - from: "GET /api/recipes"
      to: "primaryImageId field per recipe"
      via: "Prisma include with where: { order: 0 }, select: { id: true }"
      pattern: "primaryImageId"
    - from: "GET /api/recipes/[id]"
      to: "images: [...] field"
      via: "Prisma include orderBy order asc, select metadata only"
      pattern: "images:"
---

<objective>
Build the HTTP surface for multi-image: list/upload, binary fetch, delete, reorder. Extend the existing recipe list and recipe detail routes to include image metadata (and a primaryImageId convenience field for the list) without ever returning image bytes through JSON.

Purpose: The TanStack hooks in plan 03 will call these endpoints. The components in plans 04/05/06 will render against the metadata that these routes return.

Output: Three new route files, three new test files, and surgical modifications to two existing route files (recipes list + recipe detail) plus their tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-multi-image-support/02-CONTEXT.md
@.planning/phases/02-multi-image-support/02-RESEARCH.md
@.planning/phases/02-multi-image-support/02-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/app/api/recipes/[id]/image/route.ts
@src/app/api/recipes/[id]/route.ts
@src/app/api/recipes/route.ts
@src/lib/storage/storage-adapter.ts
@.planning/phases/02-multi-image-support/02-01-SUMMARY.md

<interfaces>
<!-- Contracts produced by plan 01 (StorageAdapter extensions). -->

```typescript
// From src/lib/storage/storage-adapter.ts (extended in plan 01)
saveRecipeImage(recipeId: string, data: Buffer, metadata: StorageMetadata, order: number): Promise<string>; // returns generated imageId
getRecipeImage(imageId: string): Promise<StoredImage | null>;
deleteRecipeImage(imageId: string, recipeId: string): Promise<void>; // throws P2025 on cross-recipe mismatch
listRecipeImages(recipeId: string): Promise<Array<{ id: string; order: number; fileName: string; mimeType: string }>>;
reorderRecipeImages(recipeId: string, orderedIds: string[]): Promise<void>;
```

<!-- Contract for THIS plan's outputs (consumed by plan 03 hooks). -->

```typescript
// POST /api/recipes/[id]/images
// Body: multipart/form-data with one or more `images` parts
// 201 → { imageIds: string[]; failed: Array<{ fileName: string; error: string }> }
// 400 → { error: 'No image files provided' }
// 500 → { error: 'Failed to upload images' }

// GET /api/recipes/[id]/images
// 200 → { images: Array<{ id: string; order: number; fileName: string; mimeType: string }> }

// GET /api/recipes/[id]/images/[imageId]
// 200 → binary body, Content-Type: image/*, Cache-Control: public, max-age=3600
// 404 → { error: 'Image not found' }

// DELETE /api/recipes/[id]/images/[imageId]
// 200 → { ok: true }
// 404 → { error: 'Image not found' }

// PATCH /api/recipes/[id]/images/reorder
// Body: { orderedIds: string[] }
// 200 → { ok: true }
// 400 → { error: 'Invalid request data', details: z.ZodError['errors'] }
// 500 → { error: 'Failed to reorder images' }

// GET /api/recipes (list) — adds per recipe:
// primaryImageId: string | null

// GET /api/recipes/[id] (detail) — adds:
// images: Array<{ id: string; order: number; fileName: string; mimeType: string }>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create POST + GET handlers for /api/recipes/[id]/images (multi-file upload + metadata list)</name>
  <read_first>
    - src/app/api/recipes/[id]/image/route.ts (entire file — PUT handler is the analog for multi-file POST; GET binary handler is the analog for the binary route created in task 2)
    - src/lib/storage/index.ts or src/lib/storage/storage-adapter.ts (confirm the storageAdapter singleton import shape used by existing image route)
    - .planning/phases/02-multi-image-support/02-RESEARCH.md (Pattern 2 — multi-file upload route; Pitfall 2 — concurrent upload race mitigation)
    - .planning/phases/02-multi-image-support/02-PATTERNS.md ("src/app/api/recipes/[id]/images/route.ts" section)
    - .planning/codebase/CONVENTIONS.md (Error Handling — API Routes Pattern)
  </read_first>
  <behavior>
    - Test A — happy path multi-file: POST with two valid files returns 201 with `imageIds: [id1, id2]` and `failed: []`; storageAdapter.saveRecipeImage was called twice with sequential order values starting at the recipe's current max+1
    - Test B — empty body: POST with no `images` parts returns 400 `{ error: 'No image files provided' }`
    - Test C — partial failure: POST with one valid + one oversized file returns 201 with `imageIds: [validId]` and `failed: [{ fileName: 'big.jpg', error: <validateImage error string> }]`; the oversized file did NOT call saveRecipeImage
    - Test D — server error: saveRecipeImage throws (e.g., DB outage); handler returns 500 with `{ error: 'Failed to upload images' }` and `console.error` was called with the URL path prefix `POST /api/recipes/{id}/images error:`
    - Test E — GET list: returns 200 with `{ images: [...] }` sorted ascending by order, metadata only
    - Test F — order starts at correct offset: when prisma.recipeImage.aggregate returns `_max: { order: 4 }`, the first saved image gets order=5
    - Test G — concurrency: T-02-12 documented behavior. Two POSTs that both read `_max: { order: -1 }` (i.e. happen interleaved before either persists) BOTH succeed and BOTH produce a row at `nextOrder=0` (duplicate). The test asserts: (i) neither request returns 5xx, (ii) saveRecipeImage was called twice with `order=0`, (iii) `listRecipeImages` returns the duplicates in a deterministic order — by id ascending OR by createdAt ascending (whatever the mock is configured to emit; the contract is "no crash, both rows persist, duplicates allowed"). Test name MUST include the substring `T-02-12` so the trace is greppable. (WARNING 2: this formalizes the accepted-risk concurrent-upload race rather than leaving it implicit.)
  </behavior>
  <action>
    Create src/app/api/recipes/[id]/images/route.ts (kebab-case unchanged; route segments use Next.js dynamic syntax). Match `src/app/api/recipes/[id]/image/route.ts` style verbatim:

    Imports (top of file, in this order per CONVENTIONS.md Import Organization):
    - `import { NextRequest, NextResponse } from 'next/server'`
    - `import { storageAdapter } from '@/lib/storage'`  (same import path used by existing image/route.ts)
    - `import { prisma } from '@/lib/prisma'`  (needed for aggregate query)

    GET handler signature (matches existing handler shape):
    ```
    export async function GET(
      request: NextRequest,
      { params }: { params: Promise<{ id: string }> }
    )
    ```
    Body:
    - `const { id } = await params`
    - `const images = await storageAdapter.listRecipeImages(id)`
    - Return `NextResponse.json({ images })`
    - Catch block follows the canonical error pattern (CONVENTIONS.md), including `console.error(\`GET /api/recipes/${id}/images error:\`, error)` and `NextResponse.json({ error: 'Failed to fetch images' }, { status: 500 })`

    POST handler:
    - Signature identical shape to GET
    - `const formData = await request.formData()`
    - `const files = formData.getAll('images').filter((f): f is File => f instanceof File)`  (RESEARCH Pattern 2 uses `getAll('images') as File[]`; we tighten with a type guard since FormDataEntryValue can be a string)
    - If `files.length === 0`: return 400 `{ error: 'No image files provided' }`
    - Compute next order: `const agg = await prisma.recipeImage.aggregate({ where: { recipeId: id }, _max: { order: true } })` and `let nextOrder = (agg._max.order ?? -1) + 1`
    - `const imageIds: string[] = []` and `const failed: Array<{ fileName: string; error: string }> = []`
    - Loop sequentially over `files`:
      - `const buffer = Buffer.from(await file.arrayBuffer())`
      - `const metadata = { mimeType: file.type, fileName: file.name, size: buffer.length }`
      - Call `await storageAdapter.validateImage(buffer, metadata)`. If `!isValid`, push `{ fileName: file.name, error: errors.join(', ') }` to `failed` and continue (do NOT increment nextOrder for failed files).
      - Else `const newId = await storageAdapter.saveRecipeImage(id, buffer, metadata, nextOrder++); imageIds.push(newId)`
    - Return `NextResponse.json({ imageIds, failed }, { status: 201 })`
    - Catch block: `const { id } = await params; console.error(\`POST /api/recipes/${id}/images error:\`, error); return NextResponse.json({ error: 'Failed to upload images' }, { status: 500 })`

    Concurrency note (RESEARCH Pitfall 2 / threat T-02-12): nextOrder is read once per request. Concurrent uploads to the same recipe can produce duplicate order values. For Phase 2, this is acceptable per RESEARCH "Common Pitfalls" (single-user app); we do not introduce a row lock or temporary-order scheme. Document this in the route's leading JSDoc comment with the threat ID T-02-12 referenced verbatim. The behavior is exercised by Test G.

    Create src/app/api/recipes/[id]/images/__tests__/route.test.ts:
    - `jest.mock('@/lib/storage', () => ({ storageAdapter: { listRecipeImages: jest.fn(), validateImage: jest.fn(), saveRecipeImage: jest.fn() } }))`
    - `jest.mock('@/lib/prisma', () => ({ prisma: { recipeImage: { aggregate: jest.fn(), findFirst: jest.fn(), findMany: jest.fn(), create: jest.fn(), delete: jest.fn(), update: jest.fn() } } }))` — full mock surface declared so any test that needs an additional method does not have to re-mock (checker WARNING 4).
    - Add `beforeEach(() => { jest.clearAllMocks() })` at the top of the describe block (checker WARNING 4 — isolates mock call-counts between tests).
    - For each behavior A-F, construct a `NextRequest` via `new NextRequest('http://test/api/recipes/r1/images', { method: 'POST', body: formData })` (FormData constructed with two File objects via `new File([bytes], 'name.jpg', { type: 'image/jpeg' })`)
    - Use `await GET(req, { params: Promise.resolve({ id: 'r1' }) })` style to invoke handlers directly.
    - For Test D (server error), make `saveRecipeImage` reject and assert `console.error` was called with the prefix string.
    - For Test F, `aggregate.mockResolvedValue({ _max: { order: 4 } })` and assert `saveRecipeImage` first call had `order=5`.
    - For Test G (`T-02-12 concurrency`), simulate two interleaved calls:
      ```
      it('T-02-12: concurrent uploads with same nextOrder both succeed (accepted race)', async () => {
        aggregate.mockResolvedValue({ _max: { order: -1 } })
        validateImage.mockResolvedValue({ isValid: true, errors: [] })
        saveRecipeImage.mockResolvedValueOnce('img-A').mockResolvedValueOnce('img-B')
        const reqA = makeRequest([new File(['a'], 'a.jpg', { type: 'image/jpeg' })])
        const reqB = makeRequest([new File(['b'], 'b.jpg', { type: 'image/jpeg' })])
        const [resA, resB] = await Promise.all([POST(reqA, ctx), POST(reqB, ctx)])
        expect(resA.status).toBe(201)
        expect(resB.status).toBe(201)
        expect(saveRecipeImage).toHaveBeenCalledTimes(2)
        const firstOrder = saveRecipeImage.mock.calls[0][3]
        const secondOrder = saveRecipeImage.mock.calls[1][3]
        expect(firstOrder).toBe(0)
        expect(secondOrder).toBe(0) // accepted duplicate per T-02-12
      })
      ```

    Constraints:
    - Match existing file style for image/route.ts (semicolons + single quotes per the file). Do not introduce new helper modules.
    - Do NOT use `formData.get('image')` (singular) — must be `getAll('images')` (plural form field name).
    - Do NOT return any field named `data` in either GET or POST response payload.
  </action>
  <verify>
    <automated>pnpm test --testPathPattern="api/recipes/.*images/__tests__/route" --silent 2>&1 | tail -30 && pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - All seven behavioral tests (A-G) PASS for this route
    - `grep -E "export async function (GET|POST)" 'src/app/api/recipes/[id]/images/route.ts'` matches exactly twice
    - `grep -F "formData.getAll('images')" 'src/app/api/recipes/[id]/images/route.ts'` matches
    - `grep -E "saveRecipeImage" 'src/app/api/recipes/[id]/images/route.ts'` matches at least once
    - `grep -F "T-02-12" 'src/app/api/recipes/[id]/images/route.ts'` matches (concurrency JSDoc reference)
    - `grep -F "T-02-12" 'src/app/api/recipes/[id]/images/__tests__/route.test.ts'` matches (test name)
    - `grep -F "jest.clearAllMocks" 'src/app/api/recipes/[id]/images/__tests__/route.test.ts'` matches (mock isolation)
    - The full prisma.recipeImage mock surface is present: `grep -E "aggregate:\s*jest\.fn\(\)" 'src/app/api/recipes/[id]/images/__tests__/route.test.ts'` AND `grep -E "findFirst:\s*jest\.fn\(\)" ...` AND `grep -E "findMany:\s*jest\.fn\(\)" ...` AND `grep -E "create:\s*jest\.fn\(\)" ...` AND `grep -E "delete:\s*jest\.fn\(\)" ...` AND `grep -E "update:\s*jest\.fn\(\)" ...` all match
    - Negative grep: no `data:` field returned in route responses. `grep -nE "json\(\{[^}]*\bdata\b" 'src/app/api/recipes/[id]/images/route.ts'` returns nothing
    - `pnpm exec tsc --noEmit` exits 0 (no masking)
  </acceptance_criteria>
  <done>Multi-file upload route created with order calc, validation reuse, per-file failure reporting; GET list route returns metadata only; tests cover seven behaviors including the documented T-02-12 race.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create GET (binary) + DELETE handlers for /api/recipes/[id]/images/[imageId]</name>
  <read_first>
    - src/app/api/recipes/[id]/image/route.ts (lines 4-34 for binary GET; lines 72-88 for DELETE — both are exact analogs)
    - .planning/phases/02-multi-image-support/02-RESEARCH.md (Anti-patterns — cross-recipe guard; Security Domain — V4)
    - .planning/phases/02-multi-image-support/02-PATTERNS.md ("src/app/api/recipes/[id]/images/[imageId]/route.ts" section)
    - .planning/codebase/CONVENTIONS.md (Error Handling — API Routes; Logging)
  </read_first>
  <behavior>
    - Test A — binary GET happy path: returns 200 with `Content-Type: image/jpeg`, `Content-Length` matching buffer.byteLength, `Cache-Control: public, max-age=3600`; body bytes equal the buffer
    - Test B — binary GET cross-recipe mismatch: imageId exists but belongs to a different recipe; handler queries prisma.recipeImage.findFirst with `{ id: imageId, recipeId: id }` and returns 404 `{ error: 'Image not found' }` (no 403, to avoid leaking imageId existence)
    - Test C — binary GET missing imageId: returns 404
    - Test D — DELETE happy path: calls `storageAdapter.deleteRecipeImage(imageId, id)` and returns 200 `{ ok: true }`
    - Test E — DELETE cross-recipe mismatch: storageAdapter.deleteRecipeImage throws P2025; handler returns 404 `{ error: 'Image not found' }`
    - Test F — DELETE server error: storageAdapter.deleteRecipeImage throws an unrelated error; handler returns 500 with the canonical error envelope
  </behavior>
  <action>
    Create src/app/api/recipes/[id]/images/[imageId]/route.ts:

    Imports:
    - `import { NextRequest, NextResponse } from 'next/server'`
    - `import { storageAdapter } from '@/lib/storage'`
    - `import { prisma } from '@/lib/prisma'`

    GET handler (binary):
    - Signature: `export async function GET(request: NextRequest, { params }: { params: Promise<{ id: string; imageId: string }> })`
    - `const { id, imageId } = await params`
    - Ownership check: `const ownership = await prisma.recipeImage.findFirst({ where: { id: imageId, recipeId: id }, select: { id: true } })`. If null → return `NextResponse.json({ error: 'Image not found' }, { status: 404 })` (per RESEARCH Anti-patterns: 404 not 403).
    - Fetch bytes: `const image = await storageAdapter.getRecipeImage(imageId)`. If null → 404.
    - Return binary response copy-verbatim from existing pattern:
      ```
      return new NextResponse(new Uint8Array(image.data), {
        headers: {
          'Content-Type': image.metadata.mimeType,
          'Content-Length': image.metadata.size.toString(),
          'Cache-Control': 'public, max-age=3600',
        },
      })
      ```
    - Catch block: `console.error(\`GET /api/recipes/${id}/images/${imageId} error:\`, error)` and 500 envelope.

    DELETE handler:
    - Signature mirrors GET.
    - Call `await storageAdapter.deleteRecipeImage(imageId, id)` directly (the adapter's `where: { id, recipeId }` already enforces cross-recipe).
    - On success: `NextResponse.json({ ok: true })`.
    - In catch: check `if (error instanceof Error && 'code' in error && (error as any).code === 'P2025') return NextResponse.json({ error: 'Image not found' }, { status: 404 })`. Else 500 envelope with `Failed to delete image` message.

    Create src/app/api/recipes/[id]/images/[imageId]/__tests__/route.test.ts:
    - Mock `@/lib/storage` (getRecipeImage, deleteRecipeImage) and `@/lib/prisma` (recipeImage.findFirst).
    - Add `beforeEach(() => { jest.clearAllMocks() })`.
    - Test A: findFirst resolves with `{ id: 'img1' }`, getRecipeImage resolves with `{ id: 'img1', data: Buffer.from([1,2,3]), metadata: { mimeType: 'image/jpeg', fileName: 'a.jpg', size: 3 } }`. Call GET, assert response.headers.get('Content-Type') === 'image/jpeg' and `await response.arrayBuffer()` length === 3.
    - Test B: findFirst resolves null (cross-recipe). Assert 404 + `error: 'Image not found'`.
    - Test C: findFirst resolves `{ id }` but getRecipeImage resolves null. Assert 404.
    - Tests D/E/F: configure deleteRecipeImage mock appropriately.

    Constraints:
    - Both handlers use the canonical try/catch envelope.
    - DELETE MUST NOT call findFirst separately — that's the adapter's job via composite where (avoids a redundant DB roundtrip).
    - GET MUST call findFirst BEFORE pulling bytes (otherwise we serve another recipe's bytes for one DB roundtrip).
  </action>
  <verify>
    <automated>pnpm test --testPathPattern="api/recipes/.*imageId.*route" --silent 2>&1 | tail -30 && pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - All six behavioral tests (A-F) PASS
    - `grep -c "findFirst" 'src/app/api/recipes/[id]/images/[imageId]/route.ts'` >= 1 (ownership guard present)
    - `grep -E "Cache-Control.*max-age=3600" 'src/app/api/recipes/[id]/images/[imageId]/route.ts'` matches
    - `grep -E "P2025" 'src/app/api/recipes/[id]/images/[imageId]/route.ts'` matches (DELETE 404 mapping)
    - Negative: `grep -cE "status:\s*403" 'src/app/api/recipes/[id]/images/[imageId]/route.ts'` is 0 (no 403 leaks)
    - `pnpm exec tsc --noEmit` exits 0 (no masking)
  </acceptance_criteria>
  <done>Binary GET + DELETE handlers created with ownership guard and Cache-Control header; tests prove cross-recipe 404 and renormalization happens through adapter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create PATCH /api/recipes/[id]/images/reorder route + tests</name>
  <read_first>
    - src/app/api/recipes/[id]/route.ts (PUT handler — zod schema + try/catch envelope at lines 94-240)
    - .planning/phases/02-multi-image-support/02-RESEARCH.md (Pattern 4 batch reorder)
    - .planning/phases/02-multi-image-support/02-PATTERNS.md ("src/app/api/recipes/[id]/images/reorder/route.ts" section)
  </read_first>
  <behavior>
    - Test A — reorder happy path: PATCH with `{ orderedIds: ['c','a','b'] }` returns 200 `{ ok: true }`; storageAdapter.reorderRecipeImages called once with `(recipeId, ['c','a','b'])`
    - Test B — reorder zod failure: PATCH with `{ orderedIds: [] }` returns 400 with `error: 'Invalid request data'` and a `details` array
    - Test C — reorder server error: storageAdapter rejects → 500 with `Failed to reorder images`
  </behavior>
  <action>
    Create src/app/api/recipes/[id]/images/reorder/route.ts (verbatim style of [id]/route.ts):

    Imports:
    - `import { NextRequest, NextResponse } from 'next/server'`
    - `import { z } from 'zod'`
    - `import { storageAdapter } from '@/lib/storage'`

    Zod schema (module-scope):
    ```
    const ReorderSchema = z.object({
      orderedIds: z.array(z.string().min(1)).min(1),
    })
    ```

    PATCH handler:
    - Signature: `export async function PATCH(request: NextRequest, { params }: { params: Promise<{ id: string }> })`
    - Try block: `const { id } = await params; const body = await request.json(); const { orderedIds } = ReorderSchema.parse(body); await storageAdapter.reorderRecipeImages(id, orderedIds); return NextResponse.json({ ok: true })`
    - Catch block follows recipe/[id]/route.ts pattern lines 220-238: branch on `z.ZodError` → 400 with `details: error.errors`; else 500 with `Failed to reorder images`. Always `console.error` with the URL path prefix.

    Create src/app/api/recipes/[id]/images/reorder/__tests__/route.test.ts:
    - Mock `@/lib/storage` with `reorderRecipeImages: jest.fn()`.
    - Add `beforeEach(() => { jest.clearAllMocks() })`.
    - Test A: configure mock to resolve, assert response 200 + body `{ ok: true }` + mock called with `('recipeId', ['c','a','b'])`.
    - Test B: send `{ orderedIds: [] }` body, assert 400 + `error: 'Invalid request data'` + `Array.isArray(body.details)`.
    - Test C: mock rejects with `new Error('boom')`, assert 500 + `error: 'Failed to reorder images'`.

    Constraints:
    - Reorder handler MUST use `storageAdapter.reorderRecipeImages`, not `prisma.$transaction` inline — the transaction lives in the adapter (plan 01).
    - Match existing file style — semicolons + single quotes.
  </action>
  <verify>
    <automated>pnpm test --testPathPattern="api/recipes/.*reorder" --silent 2>&1 | tail -30 && pnpm exec tsc --noEmit -p tsconfig.json -- 'src/app/api/recipes/[id]/images/reorder/route.ts'</automated>
  </verify>
  <acceptance_criteria>
    - Tests A, B, C PASS in src/app/api/recipes/[id]/images/reorder/__tests__/route.test.ts
    - `grep -E "ReorderSchema|orderedIds:\s*z\.array" 'src/app/api/recipes/[id]/images/reorder/route.ts'` matches
    - `grep -E "export async function PATCH" 'src/app/api/recipes/[id]/images/reorder/route.ts'` matches
    - `grep -F "storageAdapter.reorderRecipeImages" 'src/app/api/recipes/[id]/images/reorder/route.ts'` matches
    - `pnpm exec tsc --noEmit` exits 0 (no masking)
  </acceptance_criteria>
  <done>Reorder route created with zod validation, delegating to adapter; three behavioral tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Extend recipes list route to project primaryImageId</name>
  <read_first>
    - src/app/api/recipes/route.ts (entire file — list query lines 82-97; serialization lines 100-126)
    - .planning/phases/02-multi-image-support/02-RESEARCH.md (Pattern 6 N+1-safe primary image; Pitfall 5 do not leak bytes)
    - .planning/phases/02-multi-image-support/02-PATTERNS.md ("src/app/api/recipes/route.ts" section)
  </read_first>
  <behavior>
    - Test D1 — primaryImageId set: a recipe whose image include returns `images: [{ id: 'img-primary' }]` surfaces `primaryImageId === 'img-primary'` on the serialized response and NO key named `images` on that recipe
    - Test D2 — primaryImageId null: a recipe with `images: []` surfaces `primaryImageId === null` and NO key named `images`
    - Test D3 — no bytes: the Prisma include passed to `prisma.recipe.findMany` selects only `{ id: true }` on images (asserted by inspecting the mock call args); no field named `data: true` appears anywhere in the include for this route
  </behavior>
  <action>
    Modify src/app/api/recipes/route.ts (the list endpoint):
    - Locate the `prisma.recipe.findMany(...)` call (lines ~82-97 per PATTERNS).
    - Add to its `include` block: `images: { where: { order: 0 }, take: 1, select: { id: true } }`.
    - In the serialization map (lines ~100-126), for each recipe replace the raw images array with `primaryImageId`:
      - Compute `const primaryImageId = recipe.images?.[0]?.id ?? null`
      - Return an object via destructuring so the raw `images` array does NOT appear on the response: `const { images: _images, ...rest } = recipe; return { ...rest, primaryImageId }`.
    - Do NOT include `data` in any selection.

    Create/extend src/app/api/recipes/__tests__/route.test.ts:
    - If the file is absent, scaffold it with `jest.mock('@/lib/prisma', () => ({ prisma: { recipe: { findMany: jest.fn(), count: jest.fn() }, recipeImage: { aggregate: jest.fn(), findFirst: jest.fn(), findMany: jest.fn(), create: jest.fn(), delete: jest.fn(), update: jest.fn() } } }))`.
    - Add `beforeEach(() => { jest.clearAllMocks() })`.
    - Test D1: configure `findMany.mockResolvedValue([{ id: 'r1', title: 'A', images: [{ id: 'img-primary' }] /* + other required fields */ }])` and `count.mockResolvedValue(1)`. Call GET, parse body, assert `body.recipes[0].primaryImageId === 'img-primary'` AND `'images' in body.recipes[0] === false`.
    - Test D2: configure `findMany.mockResolvedValue([{ id: 'r2', title: 'B', images: [] }])`. Assert `body.recipes[0].primaryImageId === null` AND no `images` key.
    - Test D3: after invoking GET, inspect `findMany.mock.calls[0][0]` and assert that `mock.calls[0][0].include.images` equals `{ where: { order: 0 }, take: 1, select: { id: true } }`. Negative-shape: assert no key `data` appears anywhere in the serialized include argument: `expect(JSON.stringify(findMany.mock.calls[0][0].include)).not.toMatch(/"data":/)`.

    Constraints:
    - The recipe list response must NOT include an `images` array — only `primaryImageId`.
    - Do NOT remove any pre-existing list serialization fields (createdAt, updatedAt formatting, etc.).
    - Match existing file style — semicolons + single quotes.
  </action>
  <verify>
    <automated>pnpm test --testPathPattern="src/app/api/recipes/__tests__/route" --silent 2>&1 | tail -30 && pnpm exec tsc --noEmit -p tsconfig.json -- 'src/app/api/recipes/route.ts'</automated>
  </verify>
  <acceptance_criteria>
    - Tests D1, D2, D3 PASS
    - `grep -F "primaryImageId" src/app/api/recipes/route.ts` matches at least once in the serialization block
    - `grep -E "where:\s*\{\s*order:\s*0\s*\}" src/app/api/recipes/route.ts` matches
    - `grep -E "take:\s*1" src/app/api/recipes/route.ts` matches inside the images include
    - Negative: `grep -E "select:\s*\{[^}]*data:\s*true" src/app/api/recipes/route.ts` returns nothing (no bytes leak)
    - Test D3 explicitly asserts the include shape on the mock call (verified by grep on the test file: `grep -F "include.images" 'src/app/api/recipes/__tests__/route.test.ts'` matches)
    - `pnpm exec tsc --noEmit` exits 0 (no masking)
  </acceptance_criteria>
  <done>Recipes list endpoint projects primaryImageId, drops the raw images array, and is asserted byte-free; three behavioral tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Extend recipe detail route to include the full images array</name>
  <read_first>
    - src/app/api/recipes/[id]/route.ts (entire file — GET handler include block lines 39-46; serialization lines 69-83)
    - .planning/phases/02-multi-image-support/02-RESEARCH.md (Pitfall 5 do not leak bytes)
    - .planning/phases/02-multi-image-support/02-PATTERNS.md ("src/app/api/recipes/[id]/route.ts" section)
  </read_first>
  <behavior>
    - Test E1 — images array present and ordered: GET `/api/recipes/r1` returns `images: [{ id:'a', order:0, fileName:'a.jpg', mimeType:'image/jpeg' }, { id:'b', order:1, fileName:'b.jpg', mimeType:'image/png' }]` in the body; length === 2 and order ascending
    - Test E2 — no data bytes in the include or response: `findUnique.mock.calls[0][0].include.images` has shape `{ orderBy: { order: 'asc' }, select: { id: true, order: true, fileName: true, mimeType: true } }`. The response body string does not contain a `"data":` substring.
  </behavior>
  <action>
    Modify src/app/api/recipes/[id]/route.ts (the detail endpoint):
    - Locate the GET handler include block (lines ~39-46).
    - Add `images: { orderBy: { order: 'asc' }, select: { id: true, order: true, fileName: true, mimeType: true } }` to the include.
    - The serialized response already spreads the recipe; the `images` array passes through unchanged. Confirm there is NO Date serialization needed for image rows (no date fields are exposed in the select).

    Create/extend src/app/api/recipes/[id]/__tests__/route.test.ts:
    - If absent, scaffold with `jest.mock('@/lib/prisma', () => ({ prisma: { recipe: { findUnique: jest.fn(), update: jest.fn(), delete: jest.fn() }, recipeImage: { aggregate: jest.fn(), findFirst: jest.fn(), findMany: jest.fn(), create: jest.fn(), delete: jest.fn(), update: jest.fn() } } }))`.
    - Add `beforeEach(() => { jest.clearAllMocks() })`.
    - Test E1: configure `findUnique.mockResolvedValue({ id:'r1', title:'X', images:[{id:'a',order:0,fileName:'a.jpg',mimeType:'image/jpeg'},{id:'b',order:1,fileName:'b.jpg',mimeType:'image/png'}] /* + other required fields */ })`. Call GET, assert `body.images.length === 2`, `body.images[0].id === 'a'`, `body.images[1].order === 1`.
    - Test E2: from Test E1, additionally assert the include shape on the mock call: `expect(findUnique.mock.calls[0][0].include.images).toEqual({ orderBy: { order: 'asc' }, select: { id: true, order: true, fileName: true, mimeType: true } })`. And `expect(JSON.stringify(body)).not.toMatch(/"data":/)`.

    Constraints:
    - Do NOT change PUT/DELETE handler behavior in this file.
    - Match existing file style — semicolons + single quotes.
  </action>
  <verify>
    <automated>pnpm test --testPathPattern="src/app/api/recipes/\\[id\\]/__tests__/route" --silent 2>&1 | tail -30 && pnpm exec tsc --noEmit -p tsconfig.json -- 'src/app/api/recipes/[id]/route.ts'</automated>
  </verify>
  <acceptance_criteria>
    - Tests E1, E2 PASS
    - `grep -E "orderBy:\s*\{\s*order:\s*['\"]asc['\"]" 'src/app/api/recipes/[id]/route.ts'` matches inside the GET include block
    - `grep -E "select:\s*\{\s*id:\s*true,\s*order:\s*true" 'src/app/api/recipes/[id]/route.ts'` matches
    - Negative: `grep -E "select:\s*\{[^}]*data:\s*true" 'src/app/api/recipes/[id]/route.ts'` returns nothing
    - `pnpm exec tsc --noEmit` exits 0 (no masking)
  </acceptance_criteria>
  <done>Recipe detail endpoint returns a sorted images metadata array with no bytes; two behavioral tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → API route | Multipart upload, JSON reorder payload, dynamic-segment imageId/recipeId |
| API route → Database | All writes via Prisma client; storageAdapter funnels image bytes through validateImage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | POST .../images formData | mitigate | `formData.getAll('images')` filtered by `f instanceof File` type guard; each file passes `validateImage` (MIME allowlist + magic bytes + 10MB cap) before saveRecipeImage |
| T-02-08 | Information Disclosure | GET .../images/[imageId] binary | mitigate | Ownership-guard query `findFirst({ where: { id: imageId, recipeId: id }})` returns 404 on mismatch (not 403) — avoids confirming imageId existence to an unauthorized caller |
| T-02-09 | Elevation of Privilege | PATCH .../reorder, DELETE .../[imageId] | mitigate | Composite `where: { id, recipeId }` enforced inside `storageAdapter.reorderRecipeImages` and `deleteRecipeImage` (plan 01). Cross-recipe imageId yields P2025 → mapped to 404 by DELETE handler. Reorder array containing a cross-recipe id causes the whole transaction to fail (atomic rollback). |
| T-02-10 | DoS | POST .../images with many oversized files | mitigate | `validateImage` runs per file BEFORE saveRecipeImage; oversized files return in `failed[]` without DB writes. (Hard per-recipe cap deferred per RESEARCH Open Question 2 (RESOLVED).) |
| T-02-11 | Information Disclosure | GET /api/recipes (list) | mitigate | `select: { id: true }` on `images` include — never `data: true`. Serializer projects to `primaryImageId` and drops the raw images array (negative grep enforced in acceptance for Task 4; Test D3 asserts on mock call args) |
| T-02-12 | Integrity | Concurrent POST .../images | accept | Read-then-write on max(order) can produce duplicate order values under concurrent uploads; single-user app makes this low-risk (RESEARCH Pitfall 2). Renormalization on next delete or reorder repairs the state. Test G in Task 1 (`T-02-12 ...`) formalizes the accepted behavior. |
</threat_model>

<verification>
- All five tasks have green tests with at least the listed test count (7 + 6 + 3 + 3 + 2 = 21 new tests minimum)
- The recipe list test asserts NO `images` key on response objects and `primaryImageId` is correctly projected
- The recipe detail test asserts an `images` array sorted by order with metadata-only fields
- `pnpm exec tsc --noEmit` exits 0 across all modified files in this plan (no `|| echo` masking anywhere)
- `pnpm test` overall pass count strictly greater than 111 (the Phase 1 baseline) — i.e. the new tests are additive
</verification>

<success_criteria>
- POST/GET /api/recipes/[id]/images works for multi-file uploads and metadata listing (task 1)
- GET/DELETE /api/recipes/[id]/images/[imageId] enforce cross-recipe 404 and serve binary with cache headers (task 2)
- PATCH /api/recipes/[id]/images/reorder validates with zod and delegates to storageAdapter (task 3)
- Recipe list endpoint serializes primaryImageId (no images array, no bytes) (task 4)
- Recipe detail endpoint includes an images metadata array (no bytes) (task 5)
- No response body in any route in this plan carries image bytes outside the dedicated binary GET
- Concurrent-upload race (T-02-12) is explicitly tested as accepted behavior
</success_criteria>

<output>
After completion, create `.planning/phases/02-multi-image-support/02-02-SUMMARY.md` listing: each new route file, total new test count added (≥21), any deviation from the spec (e.g., if formData parsing required Buffer adjustments), and the exact response shape examples for downstream consumption.
</output>
